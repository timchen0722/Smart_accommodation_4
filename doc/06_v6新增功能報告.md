# v6 新增功能報告 — ABSA × 空檔通知 × 月報 × 長短租策略

> Smart_accommodation_4 · 2026-07-20(接續 `doc/05`)

---

## 一、本次修改與新增的檔案

### 新增(3 個程式 + 3 個資料產物)

| 檔案 | 角色 |
|---|---|
| `scripts/build_absa.py` | ABSA 離線預計算(12 面向詞典 + 局部情感窗口) |
| `modules/absa_sections.py` | ABSA UI:房源面向強弱項、市場痛點排行 |
| `modules/report_builder.py` | 月報彙整與 Markdown/HTML 輸出 |
| `data/_absa_listing.csv` (2.2 MB) | 每房源 × 面向統計 |
| `data/_absa_market.csv` | 全市 / 行政區面向基準 |
| `data/_absa_examples.csv` | 288 則代表性負評例句 |

### 修改(5 個)

| 檔案 | 修改內容 |
|---|---|
| **`modules/notify_center.py`** | ⚠️ **經你授權**:新增空檔警示觸發條件(詳見 §三) |
| `modules/portfolio_sections.py` | 新增 `render_tenure_strategy()` 長短租策略分析 |
| `pages/1_🏠_房東入口.py` | 分頁 5→6(新增 📄 月報);房源詳情底部加入 ABSA 區塊 |
| `pages/3_📊_後台分析.py` | 分頁 5→6(新增 💬 評論面向分析) |
| `scripts/build_calendar_features.py` | 新增未來 30 天空檔指標(通知用) |
| `scripts/train_backend_models.py` | 評論欄位相容(`cleaned_comments` / `comments` 皆可) |

> **資料檔異動因應**:你更新後的 `reviews_cleaned.csv.gz` 只有 `comments` 欄(原有 `cleaned_comments`),
> 已讓兩支腳本自動偵測欄位名,新舊資料皆可運行。

---

## 二、評論面向情感分析(ABSA)

### 2.1 方法(刻意不用 BERT)

| 步驟 | 做法 | 為何這樣選 |
|---|---|---|
| 面向偵測 | 12 面向 × 中英關鍵詞典 | Streamlit Cloud 約 1GB RAM,BERT/XLM-R(400MB~1.1GB)無法上線 |
| 情感判定 | 取關鍵詞前後 **±30 字元的局部窗口**計分 | 能拆解「早餐好吃**但**浴室很小」——整篇計分做不到 |
| 執行位置 | **離線預計算**,線上只讀 CSV | 24 萬則評論即時運算會逾時 |

### 2.2 實測結果(全市 24 萬則評論)

| 面向 | 提及則數 | 負評率 | 判讀 |
|---|---|---|---|
| 空調冷氣 | 7,063 | **15.7%** | 全市最大痛點 |
| 隔音噪音 | 20,184 | **13.6%** | 提及多且負評高 → 最該優先改善 |
| 空間大小 | 50,593 | **13.4%** | 期待落差(照片 vs 實際) |
| 衛浴熱水 | 18,783 | 12.4% | |
| 網路 WiFi | 4,311 | 10.0% | |
| 位置交通 | 129,478 | 0.8% | 台北優勢項 |
| 清潔 | 64,437 | 1.1% | 整體表現佳 |

### 2.3 擺放位置

| 位置 | 內容 |
|---|---|
| 房東入口 → 📋 房源詳情(頁面下方) | 此房源各面向負評率 vs 同行政區基準;自動列出 3 大痛點與 2 大強項,附具體改善建議 |
| 後台分析 → 💬 評論面向分析(新分頁) | 全市面向排行、提及熱度 × 負評率四象限圖、行政區 × 面向熱力圖、代表性負評例句 |

---

## 三、⚠️ 通知中心變更(唯一動到 `notify_center.py` 的部分)

### 3.1 變更內容

| 項目 | 變更前 | 變更後 |
|---|---|---|
| 觸發條件 | 只有「風險機率 ≥ 門檻」 | 「風險機率 ≥ 門檻」**或**「未來 30 天連續空檔 ≥ N 天」 |
| 控制項 | 1 個滑桿 | 風險門檻滑桿 + **空檔警示開關 + 天數滑桿**(可關閉,關閉後行為與原本完全相同) |
| 清單顯示 | 等級徽章 | 等級徽章 + **觸發原因徽章**(⚠️ 風險 / 📅 空檔 / ⚠️📅 兩者)+ 空檔天數 |
| 信件內容 | 風險數據 + 建議 | 依觸發原因調整標題與內文;空檔房源加入空檔明細與**促銷力度建議**;整合 ABSA 痛點 |
| 通知紀錄 | 7 欄 | 8 欄(新增「觸發原因」) |

### 3.2 為何用「未來 30 天」而非 90 天

實測比較(健康房源 5,842 間):

| 窗口 | 空檔 ≥14 天觸發數 | 占比 |
|---|---|---|
| 未來 90 天 | 3,690 間 | **63%(過度觸發)** |
| **未來 30 天** | 1,621 間 | **28%(合理)** |

原因:calendar 遠期大量顯示「可訂」是因為**房東尚未開放日曆**,不是真的沒人訂
(0–30 天已訂率 52.7%,31–90 天只有 22.9%)。近期窗口才是真實訊號。

### 3.3 觸發效果(風險門檻 0.60 + 空檔 ≥14 天)

| 條件 | 房源數 |
|---|---|
| 風險觸發 | 871 |
| 空檔觸發 | 1,621 |
| 聯集(實際通知) | 2,184 |
| **僅空檔觸發** | **1,313** |

那 1,313 間是**「模型看不出來、但檔期已經在流血」**的房源——風險分數未達標,
但未來 30 天有連續 14 天以上沒有任何訂單。UI 會特別標示這個數字。

### 3.4 相容性保證

- 空檔開關**預設開啟**,但取消勾選即完全回到原本行為
- 若未執行 `build_calendar_features.py`,自動隱藏空檔控制項並顯示提示,不報錯
- 自動寄送、手動補寄、已處理狀態、LLM/規則引擎切換等既有邏輯**未更動**

---

## 四、月報自動生成

### 4.1 位置與操作

房東入口 → **📄 月報**(新分頁)→ 選房源 → (可選)勾選使用 LLM → 按「產生月報」
→ 預覽 + 下載 **Markdown** 或 **HTML**(可直接列印存 PDF)。

### 4.2 月報內容(五節)

| 節 | 內容 | 資料來源 |
|---|---|---|
| 一、風險摘要 | 風險等級、高風險機率、預測空屋率 | `_predictions.csv` |
| 二、未來檔期進度 | 365/30/90 天已訂率、空檔天數與**機會成本估算**、空檔明細表、月度 vs 同商圈 | `_calendar_metrics.csv` |
| 三、營收最適定價 | 目前價 vs 同商圈同房型最適價格帶與年營收估算 | `peer_revenue_curve()` |
| 四、住客評論面向 | Top 3 痛點面向、負評率、改善建議 | `_absa_listing.csv` |
| 五、AI 經營建議 | LLM 生成或規則摘要 | `llm_advisor` / 規則 |

實測輸出約 1,400 字元,含完整表格。範例摘要:

> 本房源目前風險等級為 **高風險**(高風險機率 75%、預測空屋率 95%)。未來 365 天已訂 8 天
> (訂房率 2%);未來 30 天內有 21 天空檔,最長連續 21 天。以現價估算,近 30 天空檔的機會成本約
> NT$ 25,620。同商圈同房型的營收最適價格帶約 NT$ 6,994,建議調高 NT$ 5,774。

---

## 五、長短租策略分析

**位置**:後台分析 → 💼 房型獲利分析(頁面下方新增區塊)

依 calendar 的每日 `minimum_nights` 中位數把房源分五群:

| 分群 | 定義 |
|---|---|
| ① 單晚起租 | 1 晚 |
| ② 短租 | 2~3 晚 |
| ③ 中短租 | 4~6 晚 |
| ④ 週租型 | 7~27 晚 |
| ⑤ 長租型 | **≥28 晚** |

**呈現**:各群的平均已訂率、平均已訂天數、中位每晚價、中位年營收;
並自動指出「填充率最高」與「營收最高」是否為同一群——若不同,代表
**「訂得滿」與「賺得多」是兩種策略**,單價與周轉必須權衡。

**市場觀察**:34% 房源的最低入住天數逐日變動(已在做動態天數策略);
30 晚設定達 32.8 萬筆日資料,是短租長租化的明確訊號。
UI 已註明長租型的「不可訂」可能是房東封鎖短租日期而非訂滿。

---

## 六、驗證結果

| 項目 | 結果 |
|---|---|
| `build_absa.py` | ✅ 239,218 則評論 × 12 面向,產出 3 檔 |
| `build_calendar_features.py`(更新) | ✅ 新增 30 天空檔指標,3.6MB |
| 月報生成 | ✅ 實測輸出完整 Markdown(1,382 字元) |
| AppTest:`index.py` | ✅ OK |
| AppTest:房東入口(**6 分頁**) | ✅ OK(含自動寄送情境) |
| AppTest:租客入口 | ✅ OK |
| AppTest:後台分析(**6 分頁**) | ✅ OK |

---

## 七、部署步驟

```powershell
# 1. 產生新資料產物(ABSA 與更新後的檔期指標)
python -X utf8 scripts/build_absa.py
python -X utf8 scripts/build_calendar_features.py

# 2. 推送
git add .
git commit -m "v6: ABSA, gap-triggered notifications, monthly report, tenure strategy"
git push
```

---

## 八、目前完整分頁結構

| 頁面 | 分頁 |
|---|---|
| 首頁 | 動畫 Hero |
| **房東入口** | 🏠 總覽 / 📋 詳情(LIME + **ABSA**)/ 🗺 附近比較 / 🔔 通知中心(**+空檔觸發**)/ 📅 未來檔期 / **📄 月報** |
| 租客入口 | 找房比價 |
| **後台分析** | 📐 模型評估 / 🔍 SHAP / 🎯 前瞻驗證 / 💼 房型獲利(**+長短租策略**)/ **💬 評論面向** / 🔔 通知中心 |
