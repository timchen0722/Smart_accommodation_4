# 階段 9 — 重複資訊盤點（只列清單，未刪除）

2026-07-24 · 依使用者裁示「先列清單、逐條圈選，不要直接刪」。
**本檔目前沒有任何一項被執行。** 請在每項的 `- [ ]` 打勾表示「同意處理」，
不同意就留空或直接刪掉該項。

判準沿用計畫的區塊分工原則：
> 統計卡只放數字 · 摘要只講主因 · 列表只放個別房源 · AI 建議只放下一步

---

## 🔴 第一級：同一件事在畫面上self-contradicts（建議優先處理）

### D1. 房東入口:紅/黃/綠在同一頁有兩套門檻、兩種意思
- 證據:
  - `pages/1_🏠_房東入口.py:530`（風險診斷分頁）
    「環 = 高風險機率 P(空屋率≥60%)，**紅≥60% · 黃≥35% · 綠<35%**」
  - `pages/1_🏠_房東入口.py:789`（定價情報分頁的周邊地圖）
    「點位顏色 = 預測空屋率（**綠 <40% · 黃 40–69% · 紅 ≥70%**）」
- 問題:同一頁、同一組顏色，一個是「風險機率」門檻，一個是「空屋率」門檻，
  數字還完全不同。使用者無從得知地圖上的紅點和風險環的紅是不是同一件事
  （實際上不是）。這違反全站「紅=要處理，跨分頁不得變意思」的既定原則。
- 建議處置:地圖點位改用與三層警報相同的 `tier`（即 `RISK_TIERS` 的機率門檻），
  說明文字直接引用 `ui_kit.risk_legend_html()`，全站只留一套門檻說明。
- 影響:`modules/map_view.py` 的 `RISK_COLOR` 與 `pages/1:789` 文案。
  **會改變地圖上點位的顏色分佈**（門檻換了），屬視覺可見變更。
- [ ] 同意處理

### D2. 模型口徑說明散在三處，版本還不一致
- 證據:
  - `pages/1:118`（側欄）「HistGradientBoosting+XGBoost(Isotonic 校準)· 標籤
    vacancy_90 > 0.70 · LIME 可解釋 · GroupKFold 誠實驗證 · 37 核心特徵」
  - `pages/3:92-94`（後台側欄）幾乎同一段，但多了「雙層警報(紅 0.60/黃 0.35)」
    與「雙輸出:90天風險/365天營收」
  - `pages/1:531`（風險環面板底部）「模型:HistGradientBoosting·完整變體」
- 問題:同一組技術口徑抄三份，已經出現內容漂移（後台版較新）。
  日後換模又要改三個地方 —— 這正是本次整理要消滅的模式。
- 建議處置:抽成 `design_tokens.MODEL_SPEC` 或 `ui_kit.model_spec_caption()`，
  三處共用同一份；風險環面板只保留「完整/冷啟動變體」這個**該處才有的**資訊。
- 影響:純文案，不動計算。
- [ ] 同意處理

---

## 🟡 第二級：重複計數 / 重複說明（刪了畫面更乾淨，不影響理解）

### D3. 風險管理:同一批房源的筆數出現三次
- 證據:`modules/risk_cockpit_sections.py`
  - `:279` 側欄 slider「顯示筆數」（使用者自己設的值）
  - `:307` `st.caption`「符合 N 間，顯示風險分數最高的 M 間」
  - `:255` 批次列「**已選 N 間**　將對這些房源產生平台輔導通知」
- 問題:三個數字語意不同（母體/顯示/勾選）但排在一起，讀者要自己分辨。
- 建議處置:保留 `:307`（母體與顯示的關係）與 `:255`（勾選數，操作必需），
  側欄 slider 的值不再另外複述。或把三者合併成一行「符合 N ｜ 顯示 M ｜ 已選 K」。
- [ ] 同意處理

### D4. 風險管理:操作說明與批次列功能重複
- 證據:`modules/risk_cockpit_sections.py:314` note「點房源ID 展開 LIME…
  勾選左側方框後，用上方批次派信列發送」，而批次列本身（`:255`）已經寫了
  「將對這些房源產生平台輔導通知」。
- 建議處置:note 只留「點房源ID 可展開原因」，派信流程交給批次列自己說。
- [ ] 同意處理

### D5. 房東入口:「目前篩選」複述側欄選取值
- 證據:`pages/1:248` `st.caption(f"目前篩選:{_dist_txt} · {_room_txt}…")`，
  而側欄的 multiselect 本身就顯示著已選項目。
- 建議處置:只在**有篩選時**（`_filtered` 為真）顯示，且改為「已篩掉 N 間」
  這種側欄看不到的增量資訊；沒篩選時不顯示。
- [ ] 同意處理

### D6. 房東入口:LLM 建議區有 4 條並存的狀態說明
- 證據:`pages/1:618`（建議產生於調整參數前）、`:622`（由誰於何時生成）、
  `:626`（按上方按鈕即可產生）、`:630`（未設定金鑰）。
- 問題:這些是互斥狀態，但文案風格不一，且 `:626` 是在按鈕正下方重述按鈕功能。
- 建議處置:刪 `:626`（按鈕自己會說話），其餘三條保留但統一句式。
- [ ] 同意處理

### D7. 租客入口:五科計分公式在標題、caption、規則說明三處重述
- 證據:`pages/2:264`（交通分公式）、`:271`（生活分公式）、`:331`（設備分公式）、
  `:358`（口碑分公式），加上 page_header 已寫「五科成績單（各 5 分）」，
  以及 `:499` 側欄「評分規則 v1.0」。
- 問題:每科底下都貼一次算式，資訊密度高但都是同一種東西。
- 建議處置:各科 caption 保留（那是**該科專屬**的實際數值代入，不是重複），
  但把「各 5 分」「規則 v1.0」這種全域說明集中到 page_header，其餘刪除。
- 註:這一項我判斷**重複程度最低**，可能不值得動。
- [ ] 同意處理

---

## 🟢 第三級：結構順序不一致（非重複，但屬階段 9 的「六段結構」目標）

### D8. 四個分析頁的區塊順序不一致
計畫希望統一為:①標題說明 ②篩選與日期 ③關鍵指標卡 ④主要分析結果
⑤詳細資料列表 ⑥風險原因與建議行動。

現況:
| 頁面 | 現有順序 | 缺漏 |
|---|---|---|
| 後台·市場總覽 | ③→④→⑤ | 無②（篩選在側欄）、無⑥ |
| 後台·風險管理 | ⑤→⑥ | 無③關鍵指標卡 |
| 後台·營收與成長 | ③→④(①②③編號區塊) | 無⑤⑥ |
| 後台·數據分析 | ①→③→④ | 無⑤⑥（本頁是模型稽核，本就不需要） |
| 房東入口·房源總表 | ③→④→⑤ | 無⑥（建議在另一分頁） |

- 建議處置:**不強求五頁都湊滿六段**。只補「風險管理缺關鍵指標卡」一項
  （該頁一進去就是列表，看不到全局），其餘維持。
- [ ] 同意處理（只補風險管理的指標卡）
- [ ] 同意處理（嚴格套用六段結構到所有分析頁）

---

## 我沒有列入的（說明理由，避免你以為漏了）

- **各科 caption 的實際數值代入**（租客入口）:那是每科不同的計算過程，不是重複。
- **`note()` 的風險免責說明**（營收頁「所有金額皆為估算」等）:法遵性質，重複是刻意的。
- **後台四分頁各自的 `section_header` 說明文字**:每段講的是不同東西，已在階段 4–5 收斂過。
- **`AI_README.md` 與各 plan 文件的重複**:文件層，不在階段 9 的畫面範圍。
