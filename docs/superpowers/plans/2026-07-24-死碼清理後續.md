# 2026-07-24 死碼清理後續（backend_v2_sections 移除的連帶處理）

**前置**：commit `fdc4964` 已刪除 `modules/backend_v2_sections.py`（死碼），`9ae7ae6` 清掉其 `__pycache__` 產物。
本計畫處理該刪除的**連帶孤兒**與**文件債**。

## 範圍界定（重要）

| 區塊 | 位置 | 處置 | 理由 |
|---|---|---|---|
| 舊版規則基準線函式 | `ml_models.py` L1–351 | **保留** | L354 有明確 `保留作為「規則基準線」對照展示，勿刪` 註記 |
| v2 區塊（第一代） | `ml_models.py` L352–420 | 刪除 | 8 函式，零引用，與第二代完全重複（連 import 都重貼一次） |
| v2 區塊（第二代） | `ml_models.py` L422–525 | 刪除 | 9 函式，零引用，唯一消費者是已刪的 `backend_v2_sections.py` |
| `risk_cockpit_sections.py` / `ui_kit.py` 未提交修改 | 工作區 | **不碰** | 另一 session 的在途工作 |

刪除後 `ml_models.py` 僅餘上半段舊版函式，本身不再有 `import streamlit`（已確認 L1–351 零依賴 `st.` / `fe.` / v2 匯入符號）；
但仍會**間接**載入 streamlit——它 `from modules.ui_components import FEAT_ZH`，而 `ui_components` 匯入 streamlit。
唯一 live 消費者是 `modules/data_loader.py:10` 的 `compute_risk_scores`。

## 階段

### 階段 1：文件債
- [ ] `doc/05_v5改版完整結構報告.md:71` 目錄樹移除 `backend_v2_sections.py` 一行
- [ ] `scripts/train_backend_models.py:241` docstring 不再指向已刪模組

驗收：`grep -rn "backend_v2_sections" doc scripts` 僅剩歷史計畫檔（`docs/superpowers/plans/`）命中。

### 階段 2：ml_models.py 移除 v2 兩區塊
- [ ] 先備份 `modules/ml_models.py-backup-2026-07-24`
- [ ] 刪除 L352–525（17 個函式 + 重複 import）

驗收：
```
python -c "import modules.ml_models"          # 不得再拉入 streamlit
python -c "from modules.data_loader import *" # 唯一消費者仍可用
```

### 階段 3：驗證與提交
- [ ] `python -m pytest tests/ -q` → 須為 170 passed（與刪除前基準一致）
- [ ] 全 modules 逐一 import 煙霧測試
- [ ] `git diff --cached --stat` 確認未誤帶其他 session 的檔案後提交

## 已驗證的前提（勿重跑）

- 17 個 v2 函式名逐一 grep（排除自身與 `-backup-`）外部引用皆為 **0**
- `ml_models` 全專案唯一 importer：`modules/data_loader.py:10 from modules.ml_models import compute_risk_scores`
- 無 `import *`、無對 `ml_models` 的 `getattr` 動態取用
- 房東入口的「為什麼有風險」已改用 `modules/lime_explainer.py`（LIME），不再走 `local_shap_v2`
